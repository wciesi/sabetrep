// File: array_ex4.cs
using System;

class aex4
{
    static void Main()
    {
        int[] numbers = new int[] {1,2,3,4,5};

        Console.WriteLine("Before:");
        foreach(???)
        {
            Console.Write("{0,3} ", z);
        }
        Console.WriteLine();

        int y = numbers.Length - 1;
        for (int x = 0; x < numbers.Length / 2; x++, y--)
        {
            // code here
        }

        Console.WriteLine("After:");
        foreach(int z in numbers)
        {
            //code to show the result, After
        }

        Console.ReadLine();
    }
}