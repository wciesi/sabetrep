// File: Template for Student
// File: if_ex2.cs
// Write a program that checks if 
// the first character of the typed string is alphabetic or not.
// Hint: you can use IsLetter()function

using System;

class ex2
{
    static void Main()
    {
		// Ask use to type a string	
        Console.Write("Please type a string (numbers and alphabets): ");
        // code to read the user's input
        // code to read and assign the first character of the given string to a char type
			
        if (//first character is an alphabetic)
        {
            Console.WriteLine("The first character {0} is an alphabetic letter",cFirst);
        }
        else if (//if digit)
        {
            Console.WriteLine("The first character {0} is a digit",cFirst);
        }
        else // otherwise
        {
            // code to say it is neither type
        }
    }
}